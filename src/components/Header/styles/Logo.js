import styled from "styled-components/macro";
import Logo from "assets/images/parcel-logo.png";

export default styled.div`
  background: url(${Logo}) no-repeat;
  background-size: cover;
  width: 60px;
  background-position: center;
`;
